var fs = require('fs');

var content = fs.readFileSync('index.html', 'utf-8');

//SQLException

//\[u\](.*?)\[\/u\]
try {
    console.log(JSON.parse('{ "param": 1 1}'));
} catch (e) {
    console.log(e.message);
}
//\s+((\S*)="([\s\S]*?)")
//(src|vars)="([\s\S]+?)"\s+?(src|vars)="([\s\S]+?)"\s*?
var reg = /<include([\s\S]*?)>([\s\S]*?)<\/include>/gim;
var match = reg.exec(content);
console.log(match[1], '\r\n', '-------------------');
console.log(match[2]);